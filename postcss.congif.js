import tailwindcss from "@tailwindcss/vite";
import autoprefixer from "autoprefixer";
import {
    plugin
} from "postcss";

export default {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
}